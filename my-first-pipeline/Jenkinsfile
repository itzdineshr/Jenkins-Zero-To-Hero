pipeline {
  agent none
  stages {
    stage('Build') {  
      agent {
        docker { image 'maven:3.8.1-adoptopenjdk-17' }
      }
      steps {
        sh 'mvn --version'
        sh 'java --version'
      }
    }
  }
}
